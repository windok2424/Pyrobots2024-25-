package org.firstinspires.ftc.teamcode.TeleOp;
import com.qualcomm.robotcore.*;
import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.hardware.CRServo;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.hardware.DcMotorEx;
import com.qualcomm.robotcore.hardware.IMU;
import com.qualcomm.robotcore.hardware.Servo;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.DcMotorSimple;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.ServoController;
import com.qualcomm.robotcore.util.ElapsedTime;

import org.firstinspires.ftc.robotcore.external.navigation.AngleUnit;
import org.firstinspires.ftc.robotcore.external.navigation.AxesOrder;
import org.firstinspires.ftc.robotcore.external.navigation.AxesReference;

import java.util.concurrent.TimeUnit;
//the port 0 of Control Hub is IMU apparently
@TeleOp(name = "Mechoop")
public class Mechoop extends LinearOpMode {
    private IMU imu;
    // For whatever reason, this does not work when uncommented and causes EMERGENCY STOP NullPointerException crash
    // private DcMotor motorFL = hardwareMap.dcMotor.get("motorFrontLeft");
    // private DcMotor motorFR = hardwareMap.dcMotor.get("motorFrontRight");
    // private DcMotor motorBL = hardwareMap.dcMotor.get("motorBackLeft");
    // private DcMotor motorBR = hardwareMap.dcMotor.get("motorBackRight");
    DcMotorEx FL;
    DcMotorEx BL;
    DcMotorEx FR;
    DcMotorEx BR;
    DcMotorEx slide;




    @Override
    public void runOpMode() throws InterruptedException{
        // Why are these declared inside of a function instead of the class?
        FL = hardwareMap.get(DcMotorEx.class, "BR");
        BR = hardwareMap.get(DcMotorEx.class, "FL");
        FR = hardwareMap.get(DcMotorEx.class, "BL");
        BL = hardwareMap.get(DcMotorEx.class, "FR");
        slide = hardwareMap.get(DcMotorEx.class, "rem");


        Servo germany = hardwareMap.servo.get("axonpowers1");
        Servo japan = hardwareMap.servo.get("axonpowers2");
        Servo italy = hardwareMap.servo.get("outClaw");
        Servo aqua = hardwareMap.servo.get("aqua");
        Servo ruby = hardwareMap.servo.get("ruby");
        Servo kana = hardwareMap.servo.get("kana");
        Servo akane = hardwareMap.servo.get("akane");
        Servo inleft = hardwareMap.servo.get("inleft");
        Servo inright = hardwareMap.servo.get("inright");
        ElapsedTime runtime = new ElapsedTime();






        //zeroPowerBehavior makes the motors seize up when not moving so they dont slide. Like me at a party.
        // Goated comment


        FL.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        FR.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        BL.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        BR.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        imu = hardwareMap.get(IMU.class, "imu");
        slide.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);


        FL.setDirection((DcMotorSimple.Direction.REVERSE));
        //motorBL.setDirection((DcMotorSimple.Direction.REVERSE));
        BR.setDirection((DcMotorSimple.Direction.REVERSE));
        boolean first = true;
        boolean counter = true;
        int count = 0;

        int temp = 0;
        boolean first2 = false;
        int count2 = 0;

        int temp2;
        boolean first3 = false;
        int count3 = 0;

        int temp3;
        boolean first4 = false;
        int count4 = 0;

        double rotation = 0;

        waitForStart();
        int opclosval = 0;
        if(isStopRequested()) return;
        while(!isStarted()){
            IMUTelemetry();
        }
        while(opModeIsActive()) {



            //gamepad 1
            double y = -gamepad1.left_stick_y;
            double x = gamepad1.left_stick_x;
            double rx = -gamepad1.right_stick_x;
            //double lb = gamepad1.left_trigger;
            boolean slowMode = gamepad1.left_stick_button;
            boolean ja10 = gamepad1.x; //extend
            boolean transfer = gamepad2.x;


            //gamepad2
            double rempow = gamepad2.left_stick_y;
            double pain = gamepad2.right_stick_x;
            boolean grab = gamepad2.a;
            boolean lower = gamepad2.b;
            boolean drop = gamepad2.y;
            boolean slowmodetrue = gamepad1.right_bumper;
            boolean up = gamepad2.right_bumper;
            boolean actual_up = false;



            int rotateTarget;
            int linearTarget;


            //drive calc
            double powerFL = (y + x + rx);
            double powerBL = (y - x + rx);
            double powerFR = (y - x - rx);
            double powerBR = (y + x - rx);
            IMUTelemetry();
            getAngle();

            if(up ==true){
                while(true) {
                    slide.setPower(-1);
                }
            }
            if(slowmodetrue){

                BL.setPower(powerBL*0.5);
                BR.setPower(powerBR*0.5);
                FR.setPower(-powerFR*0.5);
                FL.setPower(-powerFL*0.5);

            }
            else {
                BL.setPower(powerBL);
                BR.setPower(powerBR);
                FR.setPower(-powerFR);
                FL.setPower(-powerFL);
            }
            //slid
            slide.setPower(-rempow);




            //goofy ahh claw stuff
            if(opclosval != 0) {
                ruby.setPosition(0.75 - rotation);
            }
            rotation = pain;
            if(rotation>0.2){
                rotation = 0.2;
            }
            else if(rotation<-0.2){
                rotation = -0.2;
            }






//            double powerFL = (1);
//            double powerBL = (1);
//            double powerFR = (1);
//            double powerBR = (1);

            //test grab-and-transfer mechanism. ism? isms? WOO!

            if(grab == true){

                count4++;
                grab = false;
                sleep(250);
                first4 = true;
            }

            if(lower == true){

                count3++;
                lower = false;
                sleep(250);
                first3 = true;
            }

            if(ja10 == true){

                count++;
                ja10 = false;
                sleep(250);
                first = true;
            }
            if(transfer == true){

                count2++;
                transfer = false;
                sleep(250);
                first2 = true;

            }
            opclosval = count%2;
            temp = count2%3;
            temp2 = count3%2;
            temp3 = count4%2;




            boolean res = false;

            if(temp3==0){
                inleft.setPosition(0.8);
                inright.setPosition(0.3);
            }
            if(temp3==1){
                inleft.setPosition(0.95);
                inright.setPosition(0.13);
            }

            if(temp2==0){
                if(opclosval==1) {
                    if (first3 == true) {
                        aqua.setPosition(0.7); //0.7

                    }else{
                        aqua.setPosition(0.2);
                    }
                }
            }
            if(temp2==1){
                if(opclosval==1) {
                    if (first3 == true) {
                        aqua.setPosition(1);
                    }else{
                        aqua.setPosition(0.18); // 0
                    }
                }
            }


            if(opclosval == 0){ //to unextend it
                //if(temp==0){
                    if(first == true) {

                        //aqua.setPosition(1);


                        first = false;
                        //ruby.setPosition(0.8); //1 HEREJEOWEHDOUI
                        //ruby.setPosition(0.73);

                        aqua.setPosition(0.15); //0.2/0.18

                    }


                    //ruby.setPosition(0.8); // AND HERE
//                    inleft.setPosition(1);
//                    inright.setPosition(0);


                    if(temp != 1) {
                        germany.setPosition(0.65); // 69
                        japan.setPosition(0.33); // 31
                    }
                }
//                kana.setPosition(1);
//                akane.setPosition(1);


            else{ //slides are not extended and go extend
                //if(temp==0){
                    if(first == true) {
                        //aqua.setPosition(0.70);//0.75


                        //ruby.setPosition(0.8);//0.8
//                        inleft.setPosition(0.8 );
//                        inright.setPosition(0.2);

                    }
                    first = false;

                    germany.setPosition(0.2);
                    japan.setPosition(0.8);
//                kana.setPosition(0);
//                akane.setPosition(0);
                    //sleep(300);




                }
            //DAVID FOR THE LOVE OF GOD DO NOT SWAP
            //kk gotchu
            if(temp == 0){

                kana.setPosition(0.97);
                akane.setPosition(0.97);
                //italy.setPosition(0.55);

            }

            if(temp == 1){
                if(opclosval==0) {

                    kana.setPosition(0.87);// 82
                    akane.setPosition(0.87);
                    aqua.setPosition(0.3);

                    germany.setPosition(0.8);
                    japan.setPosition(0.2);
                }
            }
            if(temp==2){
                if(first2 == true) {
                    //transfer the weird thing between claws
                    //sleep(100);
                    italy.setPosition(0);
                    //sleep(200);
                    //germany.setPosition(0.7);
                    //japan.setPosition(-0.56);
                    //aqua.setPosition(0);
                    //sleep(300);
                    //ruby.setPosition(0.9);
                    kana.setPosition(0.16);
                    akane.setPosition(0.13);
                    //sleep(300);
                    //aqua.setPosition(1);
                    //germany.setPosition(-0.95);
                    //japan.setPosition(1);
                    first2 = false;

                    BL.setPower(powerBL);
                    BR.setPower(powerBR);
                    FL.setPower(-powerFL);
                    FR.setPower(-powerFR);
                }
                //    ruby.setPosition(0.8);
                BL.setPower((powerBL / 5)*4);
                BR.setPower((powerBR / 5)*4);
                FL.setPower((-powerFL / 5)*4);
                FR.setPower((-powerFR / 5)*4);

            }
            if(drop){
                italy.setPosition(0.55);

            }else{
                italy.setPosition(0);
            }



/*            if(opclosval == 1) {
                BL.setPower((powerBL / 5)*2);
                BR.setPower((powerBR / 5)*2);
                FL.setPower((-powerFL / 5)*2);
                FR.setPower((-powerFR / 5)*2);
            }*/






        }
//slides



    }
    public double getAngle(){
        double angle = imu.getRobotYawPitchRollAngles().getYaw(AngleUnit.DEGREES);
        return angle;
    }
    public void IMUTelemetry(){
        telemetry.addData("IMU", "%.2f", getAngle());
        telemetry.update();
    }
}





// added slide code
// Servo code is in but servos are not programmed yet :( set positions of servo for intake
// Test if motors for slide need to be reversed
// Add slide code to auton
// Test if opencv works transfered from android
//
